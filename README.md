# Assembly-Install

# Linux
sudo apt-get install git &&
git clone https://github.com/NCA2008/Assembly-Install.git &&
cd Assembly-Install &&
bash assembyInstall.sh 

# Termux
pkg  install git &&
git clone https://github.com/NCA2008/Assembly-Install.git &&
cd Assembly-Install &&
bash assembyInstall.sh
